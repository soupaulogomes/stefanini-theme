<div class="footer-simple mx-auto p-4 pt-8 md:max-w-screen-xl md:p-8 md:pt-14">
  <hr class="border-t border-black">
  <div class="mt-8 flex flex-col justify-between gap-8 md:flex-row md:gap-0">
    <span class="hidden md:block"
      >© {{ 'now' | date: '%Y' }}
      {{ shop.name }}. {{ 'snippets.footer_simple.all_rights_reserved' | t -}}
    </span>
    <ul class="flex flex-col gap-4 underline md:flex-row md:gap-6">
      <li>{{ 'snippets.footer_simple.police' | t }}</li>
      <li>{{ 'snippets.footer_simple.terms' | t }}</li>
    </ul>
    <span class="block md:hidden"
      >© {{ 'now' | date: '%Y' }}
      {{ shop.name }}. {{ 'snippets.footer_simple.all_rights_reserved' | t -}}
    </span>
  </div>
</div>
